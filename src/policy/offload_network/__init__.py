"""
Offload-network policy package.

This policy always offloads tasks from client nodes to server nodes,
while remaining network-aware (only uses nodes that are reachable in
the network topology).
"""

